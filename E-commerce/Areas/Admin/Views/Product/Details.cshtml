@model ProductModel
<link rel="stylesheet" href="~/css/admin-detail.css" />


<h3>Product Details</h3>
<table class="table">
    <tr>
        <th>Name:</th>
        <td>@Model.Name</td>
    </tr>
    <tr>
        <th>Description:</th>
        <td>@Html.Raw(Model.Description)</td>
    </tr>
    <tr>
        <th>Quantity:</th>
        <td>@Model.Quantity</td>
    </tr>
    <tr>
        <th>Sold:</th>
        <td>@Model.Sold</td>
    </tr>
    <tr>
        <th>Price:</th>
        <td>$@Model.Price.ToString("N0")</td>
    </tr>
    <tr>
        <th>Picture:</th>
        <td><img src="~/media/products/@Model.Image" width="200" /></td>
    </tr>
    <tr>
        <th>Picture 2:</th>
        <td><img src="~/media/products/@Model.Image2" width="200" /></td>
    </tr>
    <tr>
        <th>Category:</th>
        <td>@Model.Category.Name</td>
    </tr>
    <tr>
        <th>Brand:</th>
        <td>@Model.Brand.Name</td>
    </tr>
</table>
@if (TempData["ErrorMessage"] != null)
{
    <div class="alert alert-danger">@TempData["ErrorMessage"]</div>
}

@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success">@TempData["SuccessMessage"]</div>
}
<h4>Variations</h4>
<table class="table table-bordered">
    <thead>
        <tr>
            <th>Materials</th>
            <th>Colors</th>
            <th>Price</th>
            <th>Quantity</th>
            <th>Size</th>
            <th>Picture</th>
            <th>Add Quantity</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var variation in Model.Variations)
        {
            <tr>
                <td>@variation.Material.Name</td>
                <td>@variation.Color.Name</td>
                <td>$@variation.Price.ToString("N0")</td>
                <td>@variation.Stock</td>
                <td>@variation.Size</td>
                <td>
                    @if (!string.IsNullOrEmpty(variation.ImageUrl))
                    {
                        <img src="~/media/variations/@variation.ImageUrl" width="100" />
                    }
                </td>
                <td>
                    <!-- Nút "+" để mở trang thêm số lượng -->
                    <a href="@Url.Action("AddQuantity", "Product", new { variationId = variation.Id })" class="btn btn-success">+</a>
                </td>
                <td>
                    <form asp-action="DeleteVariation" method="post" onsubmit="return confirm('Are you sure you want to delete this variation?');">
                        <input type="hidden" name="variationId" value="@variation.Id" />
                        <input type="hidden" name="productId" value="@Model.Id" />
                        <button type="submit" class="btn btn-danger btn-sm">Delete</button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>


<a asp-action="Index" class="btn btn-primary">Back to Product List</a>




