@model IEnumerable<CategoryModel>
@{
    ViewData["title"] = "Category Listed";

    // Paginate pager = new Paginate();

    // int pageNo = 0;

    // if(ViewBag.Pager != null)
    // {
    //     pager = ViewBag.Pager;

    //     pageNo = pager.CurrentPage;
    // }
}
<link rel="stylesheet" href="~/css/admin-index.css" />

<div class="top">
    <h3>List Category</h3>
    <a asp-action="Create" asp-controller="Category" class="add-button">Add Category</a>
</div>

<div class="table-container">
    <table class="table table-hover" id="myTable">
        <thead>
            <tr>
                <th scope="col" class="text-center">#</th>
                <th scope="col">Category Name</th>
                <th scope="col">Category Description</th>
                <th scope="col">Category Slug</th>
                <th scope="col">Category Status</th>
                <th scope="col" class="text-center">Category Manage</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td class="text-center align-middle">
                        <span class="badge bg-secondary">#@item.Id</span>
                    </td>
                    <td>@item.Name</td>
                    <td>@Html.Raw(@item.Description)</td>
                    <td>@item.Slug</td>
                    <td class="text-center">
                        @if (item.Status == 1)
                        {
                            <span class="status-badge status-active">Hiển thị</span>
                        }
                        else
                        {
                            <span class="status-badge status-inactive">Ẩn</span>
                        }
                    </td>
                    <td class="text-center align-middle">
                        <div class="btn-group" role="group">
                            <button type="button"
                                    class="btn btn-outline-secondary btn-sm"
                                    disabled
                                    title="Nothing to view">
                                <i class="fa-solid fa-eye"></i>
                            </button>
                            <a asp-action="Edit"
                               asp-controller="Category"
                               asp-route-Id="@item.Id"
                               class="btn btn-outline-warning btn-sm">
                                <i class="fa-solid fa-edit"></i>
                            </a>
                            <button type="button"
                                    class="btn btn-outline-danger btn-sm"
                                    onclick="deleteCategory('@item.Id')">
                                <i class="fa-solid fa-trash"></i>
                            </button>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
    @await Html.PartialAsync("_Pagination", ViewBag.Pager as E_commerce.Models.Paginate)

</div>
@* <partial name="_paging" model="@pager" /> *@
@* @section Scripts {
    <script>
        let table = new DataTable('#categoryTable');
    </script>
} *@

<script>
    function deleteCategory(categoryId) {
        if (confirm('Are you sure you want to delete this category?')) {
            // Redirect đến action Delete với GET
            window.location.href = '@Url.Action("Delete", "Category")?id=' + categoryId;
        }
    }
</script>