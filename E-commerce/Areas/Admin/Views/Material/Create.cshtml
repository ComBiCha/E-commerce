@model MaterialModel
@{
    ViewData["title"] = "Create Material";
}
<link rel="stylesheet" href="~/css/admin-create.css" />

<h4>Create Material</h4>
<div class="container">
    <form asp-action="Create" enctype="multipart/form-data" class="form">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <div class="form-group">
            <label>Name</label>
            <input asp-for="Name" placeholder="Name" />
            <span asp-validation-for="Name" class="text-danger" />
        </div>
        <div class="form-group">
            <button class="btn btn-primary">Create</button>
        </div>

    </form>
    <a asp-action="Index" class="back-link">Back to List Material</a>
</div>
@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}